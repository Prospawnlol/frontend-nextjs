"use client";
import React from 'react';

export type NavbarProps = {
	// types...
}

const Navbar: React.FC<NavbarProps>  = ({}) => {
	return (
		<div className='box-decoration-clone bg-gradient-to-r from-amber-300 to-pink-400 text-slate-50 scale-10'>
 			Navbar works!
			</div>
	);
};

export default Navbar;
